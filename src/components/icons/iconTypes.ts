export interface IconProps {
  width: number
  height: number
  fillColor: string
}

export enum IconType {
  home = "home",
  noteBlock = "noteBlock",
  plus = "plus",
  search = "search",
  todoBlock = "todoBlock",
  save = "save",
}
