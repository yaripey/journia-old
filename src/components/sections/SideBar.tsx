import styled from "styled-components"

const SideBarContainer = styled.div`
  flex: 0 0 200px;
  height: 100%;
`

const SideBar = () => {
  return (
    <SideBarContainer>This is a sidebar</SideBarContainer>
  )
}

export default SideBar
